# 大乘百法明门论——旧工具兼容入口

## 唯一权威规范

本项目已转为以 Codex 为主工具。根目录下的 [AGENTS.md](AGENTS.md) 是**唯一权威的项目规范文档**，包含学术诚实原则、笔记更新规则、学习流程、当前阶段指针、回填机制及用户学习画像。

使用仍会自动读取本文件的旧工具进入项目时，在执行任务、提出方案或修改文件之前，必须完整读取当前版本的 `AGENTS.md`，并按其中的触发规则继续读取当前任务所需的方法论、骨架或专题文档。

## 单一维护原则

- 本文件只承担旧工具兼容路由，不复制项目规范正文。
- 项目规范的新增与修改只写入 `AGENTS.md`。
- `AGENTS.md` 发生实质性演化时，按其中的“演化溯源”规则记录。

## 冲突处理

在同一指令层级内，如本文件、其他项目文档、旧记录或代理记忆与当前 `AGENTS.md` 不一致，以当前 `AGENTS.md` 为准；系统级、开发者级及用户当前明确指令仍按其更高优先级执行。
