# 大乘百法明门论——代理入口

## 项目规范的唯一来源

本项目始终以根目录下的 [CLAUDE.md](CLAUDE.md) 作为**唯一权威的项目规范文档**。

任何代理进入本项目后，在执行任务、提出方案或修改文件之前，都必须：

1. 完整读取当前版本的 `CLAUDE.md`；
2. 遵守其中的学术诚实原则、笔记更新规则、学习流程、当前阶段指针、回填机制及用户学习画像；
3. 按 `CLAUDE.md` 的触发规则继续读取当前任务所需的方法论、骨架或专题文档。

## 单一维护原则

- `AGENTS.md` 只承担入口与路由作用，不复制 `CLAUDE.md` 的正文。
- 项目规范、学习流程、当前阶段、跨阶段回填清单和用户学习画像只在 `CLAUDE.md` 中维护。
- 修改项目规范时，直接修改 `CLAUDE.md`，不得把新规则只写入 `AGENTS.md`。
- 除非本入口的路由职责本身发生变化，否则更新 `CLAUDE.md` 时无需同步改写 `AGENTS.md`。
- `CLAUDE.md` 发生实质性演化时，按其“演化溯源”规则记录，不在本文件另建变更日志。

## 冲突处理

在同一指令层级内，如其他项目文档、旧记录或代理记忆与当前 `CLAUDE.md` 不一致，以当前 `CLAUDE.md` 为准；系统级、开发者级及用户当前明确指令仍按其更高优先级执行。

